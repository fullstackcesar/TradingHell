export const environment = {
  production: true,
  // En producción, usar URL relativa o configurar en Dokploy
  // Si backend y frontend están en el mismo dominio con proxy: '/api'
  // Si están separados, poner la URL completa del backend
  apiUrl: '/api'
};
